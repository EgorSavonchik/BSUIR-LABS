d == c


{
        if (a < b && b > c)
	{
		a = a + b - c / x * t;
	}

	if (a <= b && b >= c || a == c)
	{
		a = a + b - c / x * t;
	}

	delete[] f;
	delete d;

	someFunc(function(), temp())

	return 0;
	return;

	break;

	continue;

	for (int i = 0; i < 5; i++) {
	}

	while (int i = 0; i < 5; i++) {
	}
	
	b = new int[5];
}
